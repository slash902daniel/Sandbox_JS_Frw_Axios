const assert = require('chai').assert;
const app = require('../app');

describe('App', function(){
    it('app should retunr something', function(){
        //assert.equal(app(), 'hello');
    });
});

